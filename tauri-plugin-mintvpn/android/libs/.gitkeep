# libbox.aar is built in CI from sing-box experimental/libbox via gomobile
# and copied into this directory before `tauri android build`. See
# .github/workflows/android.yml and scripts/build-libbox.sh.
